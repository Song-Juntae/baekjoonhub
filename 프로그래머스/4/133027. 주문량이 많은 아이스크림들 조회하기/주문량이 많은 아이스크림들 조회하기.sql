-- 코드를 입력하세요
WITH U AS (
    SELECT FLAVOR, TOTAL_ORDER
    FROM FIRST_HALF

    UNION

    SELECT FLAVOR, TOTAL_ORDER
    FROM JULY
), T3 AS (
    SELECT FLAVOR, SUM(TOTAL_ORDER)
    FROM U
    GROUP BY FLAVOR
    ORDER BY SUM(TOTAL_ORDER) DESC
)
SELECT FLAVOR
FROM T3
LIMIT 3

